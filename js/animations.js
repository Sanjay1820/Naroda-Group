// This file is now redundant as animations are handled directly in index.html with GSAP
// Keeping an empty file or basic utility to prevent console 404s if referenced elsewhere.
console.log("Animations loaded via GSAP in index.html");

// --- CUSTOM CURSOR ---
// Removed for simplicity
